---
title: "properties"
icon: "file"
description: "Array of nested properties for complex types. Each item should follow the same TypeTreeProperty structure, enabling recursive nesting."
---

import { Link } from '/snippets/tsdocs/Link.jsx';
import { PageLink } from '/snippets/tsdocs/PageLink.jsx';

# properties

## Summary

Array of nested properties for complex types. Each item should follow the same TypeTreeProperty structure, enabling recursive nesting. 

## Description

Array of nested properties for complex types. Each item should follow the same TypeTreeProperty structure, enabling recursive nesting.